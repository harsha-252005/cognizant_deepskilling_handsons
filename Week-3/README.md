# Week 3 — Spring Data JPA, Hibernate, and Spring REST

`Spring-Country-Service` is the completed mandatory example: a mapped Country entity, country-code lookup, query methods, add-country endpoint, and REST API. It uses H2 and is runnable with `mvn test` or `mvn spring-boot:run`.
