# Week 5 — React Foundations (HOL 1–5)

The `React-Foundations` exercise covers components/JSX, props, state and
events, lists and keys, and conditional rendering. It is intentionally a
small dependency-free source example that can be dropped into a Vite React
project as `src/App.jsx`.
