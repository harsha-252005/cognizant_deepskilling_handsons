# Week 6 — React Applications (HOL 6–8)

`React-Applications` demonstrates controlled forms, effect-based data loading,
client-side filtering, and component composition. Insert it into a Vite React
project and run the usual `npm install && npm run dev`.
